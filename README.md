# NER-Named-Entity-Recognition-
A Streamlit-based Named Entity Recognition (NER) app built with spaCy. It allows users to input text, visualize extracted entities with spaCy’s displaCy, and view results in an interactive table.
